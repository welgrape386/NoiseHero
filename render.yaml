services:
  - type: web
    name: noisehero
    runtime: python
    buildCommand: apt-get install -y ffmpeg && pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT